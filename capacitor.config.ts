import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sycamoresstake.young-men',
  appName: 'Sycamores Stake Young Men',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
